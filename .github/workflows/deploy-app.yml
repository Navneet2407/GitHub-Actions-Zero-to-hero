name: Deploy Flask app
 
on:
    workflow_dispatch:
        
jobs: 

    deploy:
        env:
            DOCKERHUB_USER: ${{ vars.DOCKERHUB_USER}}

        runs-on: self-hosted
        
        steps:
             - name: Chekout code
               uses: actions/checkout@v4
            
             -  name: deploy wirh docker compose
                run: docker-compose up -d --build